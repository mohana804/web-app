apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-web.fullname" . }}
  labels: {{- include "go-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "go-web.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
